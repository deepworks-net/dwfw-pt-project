name: Draft Github Release

on:
  pull_request:
    branches:
      - staging
    types: [closed]

jobs:
  update_release_draft:
    uses: deepworks-net/github.actions/.github/workflows/release-drafter.yml@v1.0.103
    with:
      config-path: .github/release-drafter.yml